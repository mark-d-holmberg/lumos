jQuery ->
  # Cough...HACK..cough!
  # $("body").on "hidden.bs.modal", ".modal", ->
  #   $(this).find(".modal-body").replaceWith("<div class='modal-body'><p></p></div>")
  #   $(this).removeData "bs.modal"

  # $("body").on "shown.bs.modal", ".modal", ->
  #   $("a[rel~=popover], .has-popover").popover(trigger: "hover")
