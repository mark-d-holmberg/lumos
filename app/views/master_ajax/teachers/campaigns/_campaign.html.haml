%tr{id: "campaign_#{campaign.id}"}
  %td= link_to campaign.name, campaign
  %td= link_to campaign.school.name, [campaign.district, campaign.school]
  %td= campaign.school_wide? ? "Yes" : "No"
  %td= campaign.active? ? "Yes" : "No"
