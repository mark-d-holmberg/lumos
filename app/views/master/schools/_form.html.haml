= simple_form_for(@school) do |f|
  = f.error_notification

  = f.input :name
  = f.association :district

  .form-group
    .col-sm-offset-2.col-sm-10
      = f.button :submit, class: 'btn btn-primary'
      = link_to 'Cancel', schools_path, class: 'btn btn-link'
